
  <manifest xmlns:android="http://schemas.android.com/apk/res/android"
      >
    <application>
        <provider
            android:name="com.oneshop.plugins.sms.OneShopSmsFileProvider"
            android:authorities="${applicationId}.oneshopsms.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/oneshopsms_provider_paths" />
        </provider>
    </application>
  </manifest>

